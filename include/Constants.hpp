#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

enum Semaphores{Count};
enum Fences{Count};
enum MemoryObjects{Count};
enum Buffers{Count};
enum Images{Count};
enum Pipelines{Count};
enum PipelineLayouts{Count};
enum DescriptorSetLayouts{Count};
enum DescriptorSets{Count};

#endif